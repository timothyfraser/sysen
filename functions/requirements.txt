# Python requirements for functions in this directory
# Install with: pip install -r requirements.txt

# Core data manipulation and numerical computing
pandas==2.3.3
numpy==2.3.5

# Scientific computing and statistics
scipy==1.16.3
statsmodels==0.14.5

# Statistical modeling
patsy==1.0.2

# Data visualization
plotnine==0.15.1
matplotlib==3.10.7

# NOTE: patchworklib is deliberately NOT listed. Its latest release (0.6.6)
# calls plotnine's ggplot.draw(return_ggplot=True), an argument plotnine
# dropped after 0.10.1 - so patchworklib only works with plotnine <= 0.10.1,
# three series behind the version these chapters are written for. There is no
# newer patchworklib. ggprocess() in functions_process_control.py detects this
# and returns the main plot on its own instead.

# dplyr-style pipelines, as taught in the Python chapters
dfply==0.3.3

# example datasets used by the Python chapters (e.g. diamonds)
seaborn==0.13.2

# gapminder dataset - imported directly by the plotnine chapter
gapminder==0.1
